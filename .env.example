# Local static preview / Vercel builds
WEB3FORMS_ACCESS_KEY=your_web3forms_access_key_here

# Cloudflare Workers: set the same value as a Worker secret/variable named
# WEB3FORMS_ACCESS_KEY (Dashboard → Settings → Variables and Secrets),
# or copy this file to .dev.vars for `wrangler dev`.
